<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <div class="container-fluid">
        <div class="collapse navbar-collapse">
            <a class="navbar-brand" href="/#">Salesforce OAuth</a>
            <!-- TODO: Fix navigation overflow (when too many flows) -->
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="/#">Home</a>
                </li>
                <% data.forEach(authFlow => { %> 
                    <li class="nav-item active">
                        <a class="nav-link" href="/#<%= authFlow.id %>"><%= authFlow.short_title %></a>
                    </li>
                <% }); %>
            </ul>
        </div>
    </div>
</nav>