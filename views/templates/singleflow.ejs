<div class="p-5 mb-4 bg-light text-dark border rounded-3" id="deviceFlow">
    <div class="card">
        <%- include('flowheader', {
            name: authFlow.name,
            title: authFlow.title, 
            image: authFlow.image, 
            protocol: authFlow.protocol,
            description: authFlow.description
        }) %> 
        <%- include('flowbody', {
            name: authFlow.name,
            config: authFlow.configuration
        }) %>
    </div>
</div>
